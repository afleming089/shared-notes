//WarmUp
//practice with BigInteger

import java.math.*;

public class WarmUp_BigInteger {
  public static void main(String[] args) {
    // declare and create bigNum of type BigInteger represents Long.MAX_VALUE


    //print out bigNum


    //add one to bigNum
    // bigNum is now larger than Long.MAX_VALUE



	//the wrong way to add one and have bigNum changed.
	//bigNum.add(BigInteger.ONE);
	//To add one use bigNum = bigNum.add(BigInteger.ONE);


	//print out bigNum




      //add two to bigNum
    //You will need to create a BigInteger object with 2 and add that variable to
    //bigNum


	//print out bigNum



      //is bigNum evenly divisible by two?.
      //use the remainder(bigIntegervariable) to do a modulo
     //use the equals(bigIntegervariable) to compare
     //if x % 2 == 0 this is primitives we need BigInteger



   }
}//WarmUp_BigInteger
